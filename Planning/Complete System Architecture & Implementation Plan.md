# 🏗️ System Architecture

```text
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              STREAMLIT UI LAYER                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────────┐  │
│  │  Document Upload │  │  Chat Interface │  │        Session Manager          │  │
│  │  (PDF/TXT ≤10MB)│  │  (Multi-turn)   │  │  • Chat History                 │  │
│  │                 │  │                 │  │  • New Chat Button              │  │
│  └────────┬────────┘  └────────┬────────┘  │  • Session Isolation            │  │
│           │                    │           └─────────────────────────────────┘  │
└───────────┼────────────────────┼────────────────────────────────────────────────┘
            │                    │
            ▼                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                             ORCHESTRATOR LAYER                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐   │
│  │                         RAGOrchestrator                                  │   │
│  │   • Coordinates all components                                           │   │
│  │   • Manages conversation context (multi-turn)                            │   │
│  │   • Handles complete answer assembly                                     │   │
│  │   • Language-aware response generation                                   │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
└─────────┬───────────────────┬────────────────────┬──────────────────────────────┘
          │                   │                    │
          ▼                   ▼                    ▼
┌──────────────────┐ ┌─────────────────┐ ┌─────────────────────────────────────────┐
│  INGESTION       │ │  RETRIEVAL      │ │           GENERATION                    │
│  PIPELINE        │ │  PIPELINE       │ │           PIPELINE                      │
│ ┌──────────────┐ │ │ ┌─────────────┐ │ │ ┌─────────────────────────────────────┐ │
│ │ LlamaParse   │ │ │ │  Language   │ │ │ │         Response Generator          │ │
│ │ (PDF + TXT)  │ │ │ │  Detector   │ │ │ │   • Cohere command-a-03-2025        │ │
│ ├──────────────┤ │ │ │ (langdetect)│ │ │ │   • Context-aware prompts           │ │
│ │ Recursive    │ │ │ ├─────────────┤ │ │ │   • Multi-turn support              │ │
│ │ Chunker      │ │ │ │  Hybrid     │ │ │ └─────────────────────────────────────┘ │
│ │ (512/60)     │ │ │ │  Searcher   │ │ │ ┌─────────────────────────────────────┐ │
│ ├──────────────┤ │ │ │ Dense+Sparse│ │ │ │         Citation Formatter          │ │
│ │ Metadata     │ │ │ ├─────────────┤ │ │ │   • Inline [1], [2] style           │ │
│ │ Extractor    │ │ │ │  Reranker   │ │ │ │   • Source references               │ │
│ └──────┬───────┘ │ │ │rerank-v3.5  │ │ │ └─────────────────────────────────────┘ │
│        │         │ │ ├─────────────┤ │ │ ┌─────────────────────────────────────┐ │
│        ▼         │ │ │   RRF       │ │ │ │         No-Result Handler           │ │
│ ┌──────────────┐ │ │ │  Fusion     │ │ │ │   "I don't have information..."     │ │
│ │  Embeddings  │ │ │ └─────────────┘ │ │ └─────────────────────────────────────┘ │
│ │ embed-v4.0   │ │ └─────────────────┘ └─────────────────────────────────────────┘
│ │ (Dense)      │ │
│ ├──────────────┤ │
│ │  FastEmbed   │ │
│ │ (Sparse/BM25)│ │
│ └──────┬───────┘ │
└────────┼─────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              STORAGE LAYER                                      │
│  ┌────────────────────────────────────────────────────────────────────────────┐ │
│  │                         Qdrant (Local Persistent)                          │ │
│  │   • Dense vectors (Cohere embed-v4.0)                                      │ │
│  │   • Sparse vectors (FastEmbed BM25)                                        │ │
│  │   • Metadata: page_number, document_name, chunk_id, language               │ │
│  └────────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                           LLM PROVIDER ABSTRACTION                              │
│  ┌──────────────────────────────────────────────────────────────────────────┐   │
│  │                        BaseLLMProvider (Abstract)                        │   │
│  │   • generate(prompt, context, chat_history) -> str                       │   │
│  │   • embed(texts) -> List[List[float]]                                    │   │
│  │   • rerank(query, documents) -> List[RankedDocument]                     │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
│                                      ▲                                          │
│                                      │                                          │
│                          ┌───────────┴───────────┐                              │
│                          │   CohereProvider      │                              │
│                          │   (Active)            │                              │
│                          │   • command-a-03-2025 │                              │
│                          │   • embed-v4.0        │                              │
│                          │   • rerank-v3.5       │                              │
│                          └───────────────────────┘                              │
│                                                                                 │
│         [Future Providers: OpenAI, Groq, Gemini - Interface Ready]              │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              LOGGING SYSTEM                                     │
│  ┌──────────────────────────────────────────────────────────────────────────┐   │
│  │   • Per-upload session log files                                         │   │
│  │   • Location: logs/upload_{timestamp}_{filename}.log                     │   │
│  │   • Levels: DEBUG, INFO, WARNING, ERROR                                  │   │
│  │   • Tracks: parsing, chunking, embedding, retrieval, generation          │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────┘
```

# 📁 Directory Structure

```text
multilingual-rag/
│
├── 📄 app.py                          # Streamlit entry point
├── 📄 requirements.txt                # Dependencies
├── 📄 .env.example                    # Environment variables template
├── 📄 .env                            # Actual environment variables (git-ignored)
├── 📄 README.md                       # Documentation
├── 📄 .gitignore                      # Git ignore file
│
├── 📁 src/
│   ├── 📄 __init__.py
│   │
│   ├── 📁 config/                     # Configuration (Pydantic Settings)
│   │   ├── 📄 __init__.py
│   │   └── 📄 settings.py             # Pydantic BaseSettings
│   │
│   ├── 📁 providers/                  # LLM Provider Abstraction
│   │   ├── 📄 __init__.py
│   │   ├── 📄 base.py                 # Abstract base class
│   │   ├── 📄 cohere_provider.py      # Cohere implementation
│   │   └── 📄 factory.py              # Provider factory
│   │
│   ├── 📁 ingestion/                  # Document Processing
│   │   ├── 📄 __init__.py
│   │   ├── 📄 parser.py               # LlamaParse wrapper
│   │   ├── 📄 chunker.py              # Recursive chunking
│   │   └── 📄 pipeline.py             # Ingestion pipeline orchestrator
│   │
│   ├── 📁 retrieval/                  # Search & Retrieval
│   │   ├── 📄 __init__.py
│   │   ├── 📄 vector_store.py         # Qdrant wrapper
│   │   ├── 📄 embeddings.py           # Dense + Sparse embedding
│   │   ├── 📄 hybrid_search.py        # Hybrid search implementation
│   │   └── 📄 reranker.py             # Cohere reranker
│   │
│   ├── 📁 generation/                 # Answer Generation
│   │   ├── 📄 __init__.py
│   │   ├── 📄 generator.py            # LLM response generation
│   │   ├── 📄 prompts.py              # Prompt templates
│   │   └── 📄 citations.py            # Citation formatting
│   │
│   ├── 📁 core/                       # Core Utilities
│   │   ├── 📄 __init__.py
│   │   ├── 📄 language.py             # Language detection
│   │   ├── 📄 models.py               # Pydantic data models
│   │   └── 📄 logging.py              # Logging configuration
│   │
│   └── 📁 orchestrator/               # Main Pipeline
│       ├── 📄 __init__.py
│       └── 📄 rag_pipeline.py         # RAGOrchestrator class
│
├── 📁 ui/                             # Streamlit Components
│   ├── 📄 __init__.py
│   ├── 📄 components.py               # Reusable UI components
│   ├── 📄 chat.py                     # Chat interface
│   ├── 📄 sidebar.py                  # Document upload sidebar
│   └── 📄 session.py                  # Session state management
│
├── 📁 data/                           # Data Storage
│   └── 📁 qdrant_storage/             # Qdrant persistent data
│
├── 📁 logs/                           # Log files
│   └── 📄 .gitkeep                    # Keep folder in git
│
└── 📁 tests/                          # Unit tests
    ├── 📄 __init__.py
    ├── 📄 test_parser.py
    ├── 📄 test_retrieval.py
    └── 📄 test_pipeline.py
```

# 🛠️ Technology Stack

Component	        Technology          	                    Justification
Framework	        Streamlit	                                Required by task
Vector DB	        Qdrant (local persistent)	                Hybrid search native support
Primary LLM	        Cohere (command-r-plus)	                    Your preference
Variants LLM	    Provider agnostic	                        Swappable via config
Embeddings	        Cohere embed-multilingual-v3.0	            Excellent Arabic+English
PDF                 Parsing	PyMuPDF (fitz)	                    Fast, reliable, page numbers
Chunking	        LangChain RecursiveCharacterTextSplitter	Battle-tested
Language Detection	langdetect / lingua-py	                    Lightweight
Sparse Vectors	    Qdrant FastEmbed (BM25)	                    Native hybrid search
Config	            Pydantic + YAML	                            Type-safe configuration


# ⏱️ Implementation Plan (6 Hours)


```text
┌─────────────────────────────────────────────────────────────────────────────────┐
│  HOUR 1: Foundation & Provider Abstraction                                      │
│  ════════════════════════════════════════════════════════════════════════════   │
│  □ Project setup (venv, requirements.txt, directory structure)                  │
│  □ Config system (config.yaml, .env, Pydantic models)                          │
│  □ Base LLM provider abstract class                                            │
│  □ Cohere provider implementation (embed + generate)                           │
│  □ Provider factory                                                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│  HOUR 2: Ingestion Pipeline                                                     │
│  ════════════════════════════════════════════════════════════════════════════   │
│  □ PDF loader with page number extraction                                       │
│  □ TXT loader with metadata                                                    │
│  □ Arabic text preprocessor (normalization)                                    │
│  □ Recursive chunker with overlap                                              │
│  □ Chunk metadata structure (doc_id, page, language)                           │
├─────────────────────────────────────────────────────────────────────────────────┤
│  HOUR 3: Retrieval Pipeline                                                     │
│  ════════════════════════════════════════════════════════════════════════════   │
│  □ Qdrant vector store wrapper (persistent)                                    │
│  □ Hybrid search (dense + sparse)                                              │
│  □ Query expander (generate variants)                                          │
│  □ Reciprocal Rank Fusion (RRF)                                                │
│  □ Language detection utility                                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│  HOUR 4: Generation Pipeline                                                    │
│  ════════════════════════════════════════════════════════════════════════════   │
│  □ Prompt templates (English/Arabic aware)                                     │
│  □ Context builder (combine chunks)                                            │
│  □ Response generator with citations                                           │
│  □ Citation formatter (page numbers, doc refs)                                 │
│  □ RAGOrchestrator (connect all pipelines)                                     │
├─────────────────────────────────────────────────────────────────────────────────┤
│  HOUR 5: Streamlit UI                                                           │
│  ════════════════════════════════════════════════════════════════════════════   │
│  □ Main app layout                                                             │
│  □ Sidebar (document upload, settings)                                         │
│  □ Chat interface with history                                                 │
│  □ Session state management                                                    │
│  □ Citation display component                                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│  HOUR 6: Integration, Testing & Deployment                                      │
│  ════════════════════════════════════════════════════════════════════════════   │
│  □ End-to-end integration testing                                              │
│  □ Arabic document testing                                                     │
│  □ Multi-chunk answer testing                                                  │
│  □ Streamlit Cloud deployment                                                  │
│  □ Final documentation                                                         │
└─────────────────────────────────────────────────────────────────────────────────┘
```


# Implementation Order Proposal
```text
Phase 1: Foundation
  ├── Step 1:  requirements.txt + .env.example + .gitignore
  ├── Step 2:  src/config/settings.py (Pydantic Settings)
  ├── Step 3:  src/core/models.py (Data models)
  └── Step 4:  src/core/logging.py (Logging system)

Phase 2: Core Utilities  
  └── Step 5:  src/core/language.py (Language detection)

Phase 3: Provider Layer
  ├── Step 6:  src/providers/base.py (Abstract base class)
  ├── Step 7:  src/providers/cohere_provider.py
  └── Step 8:  src/providers/factory.py

Phase 4: Ingestion Pipeline
  ├── Step 9:  src/ingestion/parser.py (LlamaParse + TXT)
  ├── Step 10: src/ingestion/chunker.py (Recursive chunking)
  └── Step 11: src/ingestion/pipeline.py (Orchestrator)

Phase 5: Retrieval Pipeline
  ├── Step 12: src/retrieval/embeddings.py (Dense + Sparse)
  ├── Step 13: src/retrieval/vector_store.py (Qdrant)
  ├── Step 14: src/retrieval/hybrid_search.py (RRF fusion)
  └── Step 15: src/retrieval/reranker.py

Phase 6: Generation Pipeline
  ├── Step 16: src/generation/prompts.py
  ├── Step 17: src/generation/citations.py
  └── Step 18: src/generation/generator.py

Phase 7: Orchestration
  └── Step 19: src/orchestrator/rag_pipeline.py

Phase 8: Streamlit UI
  ├── Step 20: ui/session.py (Session management)
  ├── Step 21: ui/components.py (Reusable components)
  ├── Step 22: ui/sidebar.py (Document upload)
  ├── Step 23: ui/chat.py (Chat interface)
  └── Step 24: app.py (Main entry point)

Phase 9: Deployment
  └── Step 25: Streamlit Cloud setup
```

# Visual flow

```text
┌─────────────────────────────────────────────────────────────┐
│                    RAG ORCHESTRATOR                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  INGEST DOCUMENT:                                           │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐  │
│  │ Validate │ → │  Parse   │ → │  Chunk   │ → │  Embed   │  │
│  └──────────┘   └──────────┘   └──────────┘   └────┬─────┘  │
│                                                    │        │
│                                                    ▼        │
│                                              ┌──────────┐   │
│                                              │  Store   │   │
│                                              │ (Qdrant) │   │
│                                              └──────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  QUERY:                                                     │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐  │
│  │ Detect   │ → │  Search  │ → │ Rerank   │ → │ Generate │  │
│  │ Language │   │ (Hybrid) │   │          │   │ Answer   │  │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```


# App preview

```text
┌─────────────────────────────────────────────────────────────────────────┐
│  📁 Documents              │  🔍 Multilingual RAG System               │
│  ────────────────────────  │  Ask questions about your documents...    │
│                            │  ─────────────────────────────────────    │
│  Upload Document           │                                           │
│  ┌──────────────────────┐  │  💬 Chat                            [🔄]  │
│  │ Choose PDF or TXT    │  │                                           │
│  └──────────────────────┘  │  👤 What is machine learning?             │
│  [📥 Process Document]     │                                           │
│  ────────────────────────  │  🤖 Machine learning is a type of AI...   │
│                            │     [Page 5]                              │
│  Uploaded Documents        │                                           │
│  📄 guide.pdf              │     ---                                   │
│  🇺🇸 EN • 15 chunks        │     Sources:                              │
│  ────────────────────────  │     [1] guide.pdf, Page 5                 │
│                            │                                           │
│  Actions                   │  ┌─────────────────────────────────────┐  │
│  [🗑️ Clear All]            │  │ Ask a question about your docs...   │  │
│                            │  └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```